# Engineering Log — 2026-09-11

## Require Small Commits in the Agent Contract

### Summary

The Agent Operating Contract asked for "one or more focused logical commits",
which permitted an agent to land an entire session as one commit and still be
within the letter of the rule. The project owner asked for small commits for
every unit of work, so the bullet was replaced with an explicit definition:
one commit is one unit of work, each commit stands on its own, and its
`CHANGELOG.md` and `docs/logs/` entries travel in the same commit.

### Scope

Guidance only. `AGENTS.md` and `CHANGELOG.md`. No source or behavior changes.

The subject-prefix rule records what the history already does rather than
inventing a new convention: `docs:` (11 commits), `fix:` (5), `ui:` (1) and
`feat(ui):` (1) are the prefixes in use, so those are the ones named, with
Conventional Commits as the general rule for anything new.

The existing `fix:` / `docs:` pairs — such as `43eb9c0` with `816e0b1` for
issue #31 — are explicitly still allowed: a large change may separate the code
from its longer-form documentation. The new rule forbids batching *unrelated*
units, not splitting one unit across a code and a docs commit.

### Verification

`git diff --check` passed. No JavaScript changed, so no `node --check` was
needed; `node --test tests/` was run for the session baseline and passed all
33 tests.

## Pin the Klaus Dormann 6502 Functional Test Fixture (#18)

### Purpose and Scope

`AGENTS.md` has listed "run a known 6502 functional suite, such as Klaus
Dormann's tests" under Pending Verification Work since the CPU core was
written, and Known Limitations #5 says the same. This is the first slice of
that work: pin the fixture and document it. No runner, no test invocation, and
no CPU change — those follow.

### What Was Pinned

Upstream `Klaus2m5/6502_65C02_functional_tests` at revision
`7954e2dbb49c469ea286070bf46cdd71aeb29e4b` (2020-01-05), which carries the
source version string `version 05-jan-2020`. An immutable commit SHA, not a
branch name, so the fixture cannot drift underneath the tests.

Vendored into `tests/fixtures/6502-functional/`:

| File | Bytes | Role |
|---|---|---|
| `6502_functional_test.bin` | 65536 | flat 64KB memory image the runner will execute |
| `6502_functional_test.a65` | 148497 | corresponding assembler source |
| `LICENSE.txt` | 35147 | upstream GPL-3.0 text |
| `manifest.json` | — | local provenance record; not upstream |
| `README.md` | — | attribution, address derivation, coverage limits |

Every downloaded file was verified against its **git blob SHA-1** at that
revision, not merely its size, which proves the bytes are the blobs of the
pinned commit rather than whatever the branch serves today. SHA-256 checksums
are recorded in `manifest.json` for ongoing integrity checking by the runner.

The 728KB assembler listing is deliberately **not** vendored; its bulk buys
nothing at run time. Its URL and SHA-256 are in the manifest so the addresses
below can be re-derived by anyone who fetches it.

### Addresses, and Why They Were Derived Rather Than Assumed

Addresses move between upstream revisions, so each was read out of the listing
at this revision and then cross-checked byte-for-byte against the vendored
image. Agreement between the two is what establishes that the `.bin` and the
`.lst` are the same build:

| Address | Listing | Bytes in the image |
|---|---|---|
| `$0400` entry (`start`) | line 740, `0400 : d8  start cld` | `D8 A2 FF 9A` (`CLD` / `LDX #$FF` / `TXS`) |
| `$3469` success | line 13377, `3469 : 4c6934 > jmp *` | `4C 69 34` |
| `$379D` `nmi_trap` | line 14349 | `4C 9D 37` |
| `$37A3` `res_trap` | line 14350 | `4C A3 37` |
| `$FFFA` vectors | lines 14348-14351 | `9D 37 A3 37 AB 37` |

Assembly-time configuration at this revision: `ROM_vectors = 1`,
`load_data_direct = 1`, `I_flag = 3`, `zero_page = $0A`,
`data_segment = $0200`, `code_segment = $0400`, `disable_selfmod = 0`,
`report = 0`, `disable_decimal = 0`.

### Three Findings That Constrain the Runner

1. **The reset vector is a trap, not the entry point.** With `ROM_vectors = 1`
   the image fills `$FFFA-$FFFF` and points `$FFFC` at `res_trap` (`$37A3`),
   because an unexpected RESET mid-run is an error the suite wants to catch.
   The runner must assign `PC = $0400` directly and must not call
   `CPU6502.reset()`; doing so would land in a failure trap immediately and
   read as a CPU defect that is not there. The upstream source says as much:
   "alter PC to 400 hex (code_segment) and enter a go command".
2. **Every trap is a self-loop, success included.** `$3469` is `JMP $3469` and
   the failure traps are `jmp *`, `bne *`, `bcs *` and friends. So "the PC
   stopped advancing" is only the detection mechanism; the *address* carries
   the entire pass/fail signal. A runner that treats any stopped program as a
   pass would report success for every failing test in the suite, which is
   exactly the hazard the runner checks must rule out.
3. **`report = 0` means no I/O channel is needed.** The suite self-traps rather
   than reporting through a host hook, so it runs against a bare `CPU6502` with
   its plain writable 64KB array — no monitor ROM, no 8255 PIA, no cassette
   callbacks. `disable_selfmod = 0` means parts of the image modify themselves,
   so the whole image must stay writable.

### Licensing

The emulator is MIT; this fixture is GPL-3.0-or-later. They are kept apart
deliberately: the suite is unmodified third-party *test data* in its own
directory, shipped with its own license text and its corresponding source, read
at run time by a test rather than linked into or distributed as part of the
emulator, and no AMICO 2000 source is derived from it. That is mere
aggregation, and it is how emulator projects normally vendor these tests. The
call is recorded in the fixture README so it is visible to anyone reusing the
repository.

### Verification

- All five downloads matched their upstream git blob SHA-1 exactly.
- The three vendored files re-verified against the SHA-256 values written into
  `manifest.json`.
- The image was re-read at `$0400` and `$3469` to confirm `CLD` and a
  jump-to-self at the success address.
- `manifest.json` parses as valid JSON.
- No new dependencies: there is still no `package.json`, and normal test runs
  need no network and no assembler.
- `git diff --check` passed. `node --test tests/` passed all 33 tests both
  before and after, since nothing executable changed.

## Conformance Runner for the Functional Suite (#18)

### Summary

Added `tests/helpers/6502-conformance.js`, which runs the fixture pinned
earlier today against a bare `CPU6502`, and `tests/cpu6502-functional.test.js`,
which invokes it as a normal Node regression check.

**Result: the CPU core passes the suite.** It reaches the success trap at
`$3469` after 30,646,177 instructions and 96,241,367 cycles.

On the cycle total, stated carefully. Discussion on 6502.org puts a full run at
about 96,243,729 cycles; ours is 96,241,367, short by 2,362 cycles, or 0.0025%.
Being in the right range is weak corroboration that the image ran to completion
rather than stopping early somewhere harmless — a run that ended prematurely
would be out by far more than that. It is *not* evidence that the core's timing
is correct, and the small shortfall is not evidence that it is wrong: this suite
checks results and flags, never cycles, and the reference figure is a forum
recollection that may not come from this exact revision. The core's timing is
still approximate by design (Known Limitations #4), and nothing measured today
changes that. Chasing those 2,362 cycles would need a cycle-accurate reference,
which is separate work.

### Approach

The runner instantiates `CPU6502` directly through its existing CommonJS export
and uses its plain writable 64KB array. No `Amico2000`, no monitor ROM, no 8255
PIA, no cassette traps. That isolation is possible because the suite is
assembled with `report = 0`: it signals purely by trapping and needs no I/O
channel, so nothing has to be simulated around it.

Four decisions worth recording:

- **Entry is assigned, not vectored.** `cpu.reset()` is deliberately never
  called, because the image points `$FFFC` at `res_trap` (`$37A3`). Vectoring
  through reset would land in a failure trap and read as a CPU defect that is
  not there. `tests/conformance-runner.test.js` pins this.
- **A pass is an address, not a state.** Every trap in the suite is a loop on
  itself, success included, so "stopped making progress" is only how a trap is
  *detected*. `passed` is set in exactly one branch of the classifier, and only
  from the manifest's success address, so a trap, a halt, an exception, or an
  exhausted budget cannot be mistaken for a pass.
- **The budget counts instructions, not cycles.** A core whose cycle accounting
  is broken is precisely what this suite exists to catch, so it must not be able
  to stall the runner. The budget is 100,000,000 instructions against a measured
  30,646,177 for a full pass — roughly 3x headroom, and about a second of wall
  clock if it is ever exhausted.
- **Cycle totals are reported as diagnostics only.** The suite checks results
  and flags, never timing, so its cycle count is not evidence about cycle
  accuracy. The failure report says so on the line that prints it.

### Cost

About 0.6s for the full suite, including a 16-entry instruction trace kept for
every one of the 30M instructions; the trace costs roughly 200ms of that. Cheap
enough to leave enabled by default and to keep the suite inside the ordinary
`node --test tests/` run rather than behind an opt-in flag.

### Verification

`node --test tests/cpu6502-functional.test.js` passes. Full suite:
`node --test tests/` reports 52 passing, 0 failing. `node --check` passed on
both new files, and `git diff --check` passed.

A negative control was run out-of-tree to confirm the suite is not passing
vacuously: a copy of `cpu6502.js` with the `ADC` overflow flag forced to zero
was rejected at `$062B` after 40,351 instructions, and the instruction trace
showed the `BVC` at `$060E` branching into that trap — the failing check
itself. No repository file was modified by that experiment.

## Verify the Conformance Runner's Failure Reporting (#18)

### Why This Needs Its Own Tests

A passing conformance suite is only worth what its failure path is worth. The
specific hazard here is sharp: because every trap in the suite is a loop on
itself, a runner that concluded "the program stopped, therefore it finished"
would report success for *every* failing test in the suite while looking
perfectly healthy. Nothing in a green run would reveal it.

`tests/conformance-runner.test.js` exists to make that failure mode impossible
to ship unnoticed. It drives the runner with small synthetic programs whose
outcome is known by construction, so the classifier can be tested without the
64KB fixture. This is why `runImage()` is parameterised on image, entry address
and success address rather than hard-coding the real ones.

### What Is Checked

Completion handling:

- a self-loop at the success address is a pass
- the *same* `JMP *` instruction three bytes earlier is a failure — the address
  decides, not the instruction shape

Each way a run can fail, and that it reports as a failure with a usable address:

- a failure trap, with the registers at the trap
- an illegal-opcode halt, reported at the faulting address rather than the byte
  after it, since `CPU6502.step()` has already advanced PC past the opcode when
  it gives up
- an exception thrown by the core, caught rather than escaping
- a multi-instruction loop (`NOP` / `JMP`), which is never a self-loop and so
  can only be stopped by the budget
- the same loop against a stub CPU whose `cycles` never advances, proving the
  budget does not depend on the core's own cycle counter

Then the property all of it rests on, asserted across every outcome the runner
can produce: nothing but reaching the success address is ever marked passed.

Initialisation, diagnostics and the fixture:

- entry at `$0400` with `$FFFC` pointing at a fatal trap, confirming the runner
  does not vector through reset
- the failure report contains PC, opcode, the bytes at PC, registers, decoded
  status flags, instruction and cycle counts, and the cycle line's "diagnostic
  only" qualifier
- the trace ends at the fault, is bounded by its ring buffer, and drops older
  entries
- a missing, malformed, truncated or single-byte-corrupted fixture is rejected
  before anything executes, so a fixture problem cannot be read as an emulator
  defect
- the pinned fixture's manifest still agrees with the bytes it describes

### Two Corrections

Two assertions were wrong on first run, in the test rather than the runner:

- the instruction count at a trap is 5, not 4, because the trap's own `JMP *`
  must execute once before the runner can observe that PC did not move
- `flagsToString(0x24)` is `--U--I--`; the expected literal had `I` in the
  wrong column. The bit order is `N V U B D I Z C`, matching
  `CPU6502.stateToString()`

Both expectations were corrected and the reasoning left in the test as comments.

### Verification

`node --test tests/conformance-runner.test.js` passes all 18. Full suite:
`node --test tests/` reports 52 passing, 0 failing. `node --check` and
`git diff --check` passed.

## Correction: the Conformance Work Is #18, Not #17

Today's three conformance commits were filed against `#17`. That is the wrong
issue. The two are easy to confuse because both ask for CPU tests:

- **#17 "Add automated CPU regression tests for recent fixes"** — a small Node
  harness for specific recent fixes: `JSR`/`RTS` stack byte order, NMI/IRQ frame
  order, the decimal `ADC`/`SBC` cases from #4, and base/branch cycle checks.
- **#18 "Run a 6502 conformance suite against the CPU core"** — a repeatable
  command, clear pass/fail reporting, and notes for any expected deviations.

The work done today is #18 in full. `NextSession.md` already said so ("Leave the
Klaus Dormann conformance suite (`#18`) for later"); it was not checked against
the issue list before committing.

The references in `CHANGELOG.md`, this log, and the three new test files are
corrected to `#18`. The three commit subjects still read `#17` and were left
alone deliberately, on the project owner's instruction, rather than rewriting
unpushed history. Anyone tracing `#17` through `git log --grep` will land on
these commits and should read this note.

`#17` remains genuinely open and untouched: the conformance suite gives broad
coverage but is not the targeted per-fix regression harness that issue asks for,
and it explicitly does not check instruction timing, which #17 does.

## Conformance Reference Documentation (#18)

### Summary

Added `docs/6502-conformance.md`. Issue #18 asks for "a repeatable command for
local validation, clear pass/fail reporting, and notes for any expected
deviations"; the first two are code and landed earlier today, and this is the
third plus the surrounding context needed to act on a failure.

### What It Records

- the current result, dated, with the suite revision it was produced against
- the single-suite command and its relationship to `node --test tests/`, which
  is simply that the suite is an ordinary file in `tests/` with no opt-in flag
- how a pass is decided, since that is the part a reader has to trust: traps are
  self-loops, the address is the whole signal, and reset is never vectored
- why the pass is trustworthy, covering both the runner's own checks and the
  out-of-tree negative control
- how to map a failing address back to the upstream listing, including
  upstream's own advice that the flags for the tested instruction are already
  gone by the time a flag check traps
- what the suite does **not** cover, as a table with a reason for each

### On "Expected Deviations"

Issue #18 asks for notes on expected deviations. There are none: the core passes
outright, with no skipped section, no weakened assertion, and no failure
reclassified as acceptable. The coverage table is deliberately not written as
deviations either — those are gaps in what the suite *checks*, which is a
different thing and worth keeping distinct, because a gap invites follow-up work
while a deviation invites an argument about whether it is really fine.

The decimal entry is the one to watch. The suite exercises decimal `ADC`/`SBC`
with valid BCD operands only and ignores `N`, `V` and `Z`, which is precisely
the behaviour #4 and #22 were about. `tests/cpu6502-decimal-sbc.test.js`
therefore stays load-bearing; a passing conformance run must not be read as
making it redundant.

### Verification

`node --test tests/` passes 52. `git diff --check` passed. No JavaScript
changed in this commit.

## Reconcile Guidance That Predated the Conformance Suite (#18)

### Summary

Three files still described the conformance suite as work not yet done.
`AGENTS.md` listed it twice — Known Limitations #5 said running it "would
provide broader confidence", and Pending Verification Work said to run it
"before treating stack behavior, interrupt handling, BCD arithmetic, and
page-crossing timing as settled". `NextSession.md` said "Leave the Klaus Dormann
conformance suite (`#18`) for later". `README.md` had no testing section at all.

### The Care Needed in Pending Verification Work

That bullet named four areas to be settled by running the suite, and a passing
run settles only some of them. Rewriting it as simply "done" would have been
wrong in a way that costs someone real debugging time later:

- **stack behavior** — settled
- **BCD arithmetic** — settled only for *valid* BCD operands, and only for
  results, since the suite ignores `N`, `V` and `Z` in decimal mode. The NMOS
  flag semantics behind #4 and #22 are untouched by it
- **page-crossing** — the *address arithmetic* is settled, the *timing* is not,
  because the suite never checks cycles
- **interrupt handling** — not settled at all. External IRQ/NMI delivery is a
  separate upstream suite requiring a feedback register

So the bullet now says which of the four are settled and which are not, and
explicitly warns against treating the last two as covered.

### Other Changes

`README.md` gains a Testing section with both commands, the offline/no-dependency
property, and the coverage caveat; the CPU bullet under Technical Details now
cites the passing suite instead of asserting "accurate flag handling" unsupported;
Known Limitations gains an entry for the untested CPU areas and notes that a
passing suite says nothing about timing.

`AGENTS.md` documents the single-suite command next to the existing
`node --test tests/` guidance, and points at `docs/6502-conformance.md` before
an agent concludes the suite has found a CPU defect — the likelier first
explanations being a fixture or runner problem.

`NextSession.md` records #18 as implemented and passing, notes that the issue
was deliberately left open on GitHub rather than closed, and redirects attention
to the machine layer, which is now the weakly covered half of the emulator.

### Verification

`node --test tests/` passes 52. `node --check` passed on all three test files
(unchanged in this commit but re-checked). `git diff --check` passed. No
JavaScript changed.

## Stack Frame Regression Tests (#3, #17)

Issue #17 asks for committed regression tests covering the CPU fixes that were
only ever verified with ad-hoc Node snippets. The first of those is #3, the
`push16` byte-order fix.

### Why Assert the Bytes, Not the Round Trip

A test that only checks "`JSR` then `RTS` comes back to the right address"
cannot catch the bug #3 fixed. A core that pushes the two halves in the wrong
order pulls them back in the same wrong order, so the round trip succeeds. The
defect is only observable to something that reads the frame in memory — which
the AMICO monitor does when it displays the interrupted PC, and which every
interrupt handler that wants its own return address does.

So each check asserts the individual bytes in page one at known stack
addresses. The addresses used are deliberately non-symmetric — `$1234` calling
`$ABCD`, vectors pointing at `$E35A` and `$C79C` — so a swapped pair cannot
coincidentally pass.

### What Is Covered

`tests/cpu6502-stack-frames.test.js`, nine checks:

- `JSR` pushes PC-1 high byte first, at the higher stack address
- `RTS` resumes at the byte after the operand, via the +1 on the pulled address
- nested frames unwind in order, with the inner frame's bytes checked too
- `BRK` pushes the address *past* its padding byte, with `B` set in the status
- `RTI` restores the status register and returns without the `RTS` increment
- `IRQ` pushes the not-yet-executed PC with `B` clear, masks `I`, costs 7 cycles
- `IRQ` is held off while `I` is set, and the line stays asserted
- `NMI` is taken through `$FFFA` even with `I` set, and consumes the edge
- a frame pushed at `SP = $00` wraps within page one and pulls back correctly

The `B`-clear assertions matter beyond tidiness: they are how an `RTI` handler
distinguishes a software `BRK` from a hardware interrupt.

### Verification

`node --check tests/cpu6502-stack-frames.test.js` passes.
`node --test tests/cpu6502-stack-frames.test.js` passes 9 of 9.
No emulator source changed; this commit is tests and documentation only.

## Decimal Mode Regression Tests (#4, #22, #17)

The second of #17's areas: decimal-mode `ADC`/`SBC`, fixed in #4 and refined in
#22, and previously protected by a single vector in
`tests/cpu6502-decimal-sbc.test.js`.

### A Sweep Instead of Vectors

Decimal mode is the part of this core most likely to rot quietly. The
accumulator is BCD-adjusted while `N`, `V` and `Z` follow the *binary*
arithmetic, so a plausible-looking simplification can leave every result
correct and the flags wrong — which is precisely what #4 and #22 were.

A handful of vectors would not reliably catch that. So
`tests/cpu6502-decimal-flags.test.js` restates the documented NMOS algorithm
(Bruce Clark, "Decimal mode in NMOS 6502") independently and sweeps every valid
BCD operand pair against it: 100 × 100 × 2 carry inputs = 20,000 comparisons
per instruction, each covering the result and all four affected flags.

The core agrees on all 40,000. That is meaningful because the emulator computes
the same answers by a visibly different route — it works from the binary sum
and corrects, where the reference builds the result nibble-wise — so the two
implementations share no formulation to be wrong in together.

Runtime is about 3ms for both sweeps, reusing one `CPU6502` instance; building
the instruction table 40,000 times would have dominated the measurement.

### The Sweep Is Not Sufficient On Its Own

Two implementations can still be wrong in the same way, so named vectors state
the hardware facts outright:

- `$44 + $44 = $88` sets both `N` and `V`, from the intermediate sum before the
  high-nibble correction
- `$99 + $01` wraps the accumulator to `$00` with carry, yet `Z` stays **clear**
  because the binary sum `$9A` is non-zero. Reading `Z` off the accumulator is
  the obvious wrong implementation
- `$00 - $80` with carry set leaves `$20` in the accumulator but sets `N`, from
  the binary difference — the #22 vector, kept here beside its ADC counterparts
- the same operands with `D` clear still take the binary path, so a future edit
  cannot leak the decimal correction into binary mode unnoticed

### The Boundary Is Asserted, Not Assumed

Valid BCD operands only. Real NMOS silicon produces defined results when a
nibble is `$A-$F`, and this core does not reproduce them — for `ADC $8F + $0F`
with carry, hardware gives `$95` with carry clear and this core gives `$05`
with carry set.

That gap is accepted, not newly discovered: `docs/6502-conformance.md` already
recorded it, the functional suite does not test it, and nothing in the emulator
or the monitor feeds invalid BCD to decimal arithmetic. What is new is that the
final check in the file *asserts* the divergence, so the limit is visible in the
test output instead of being an unstated assumption. If the core is ever made to
match, that check fails and says what to do: delete it and widen the sweeps to
all 256 operand values.

### Verification

`node --check tests/cpu6502-decimal-flags.test.js` passes.
`node --test tests/cpu6502-decimal-flags.test.js` passes 7 of 7 in 49ms.
`tests/cpu6502-decimal-sbc.test.js` is left in place: it pins #22's vector
specifically, and the conformance doc's reference to it stays valid.
No emulator source changed.

## Instruction Timing Regression Tests (#5, #17)

The third and last of #17's areas: the cycle-counting contract from #5.

### Why This Needs Its Own Tests

Nothing else in the repository looks at cycles. The Klaus Dormann functional
suite checks results and flags and never the clock — `docs/6502-conformance.md`
says so explicitly — so until now a timing regression could pass every check in
`tests/` unnoticed.

The core is not cycle-accurate in the strong sense: it has no model of what
happens *within* an instruction. But the per-instruction totals are a real
contract, and #5 was about getting them right.

### The Whole Table, Not a Sample

`tests/cpu6502-cycles.test.js` carries a reference table of documented NMOS
base cycles for all 151 opcodes and checks every one. It then asserts the
implemented set is exactly those 151 — no missing entry, and nothing extra.
That second assertion matters as much as the first: it would catch an
undocumented opcode quietly gaining an implementation, which would change what
"the core halts on unknown opcodes" means.

Base costs are also measured through `step()` for a sample spanning the
addressing modes, not only read off the table, so the table and the actual
charge cannot drift apart. That is the specific #5 regression: a core that
charges the table cost in `step()` *and* again inside a handler bills
everything twice, and a table-only check would not notice.

### The Penalties, Including Where They Must Not Apply

- indexed **reads** (`abs,X`, `abs,Y`, `(zp),Y`) pay one extra cycle when the
  indexed address changes page, and `(zp,X)` never does, because it indexes
  within zero page
- **stores and read-modify-write** never pay it. Their extra address cycle is
  unconditional and already in the base cost, so charging the penalty as well
  would overcharge precisely on the crossing case. This is the easiest way to
  break the timing while every result stays correct, so `STA abs,X`,
  `STA abs,Y`, `STA (zp),Y` and all five `RMW abs,X` forms are each checked on
  both sides of a page boundary
- **branches** cost 2 not taken, 3 taken within a page, 4 taken across one, in
  both directions. One case pins the subtlety that the page comparison is made
  against the address *after* the operand rather than the branch's own address:
  a `BEQ` at `$12FE` jumping to `$1310` crosses no page and costs 3

### NMOS, Not 65C02

Decimal `ADC`/`SBC` must cost exactly what binary does. The extra decimal-mode
cycle arrived with the 65C02 and the AMICO 2000 has an NMOS part. Checked
across all eight addressing modes for both instructions rather than one, since
a penalty could be introduced in the shared handler or in a single mode.

### One Recorded Choice

A final check covers the unknown-opcode halt: it charges no cycles and makes no
progress. That is a debugging choice, not NMOS behaviour — real silicon executes
the undocumented opcodes — and the test exists so the cost of the choice is
visible rather than implied. The core's `console.warn` is stubbed for the
duration, since that test halts it deliberately and the warning is noise in a
test run.

### Verification

`node --check tests/cpu6502-cycles.test.js` passes.
`node --test tests/cpu6502-cycles.test.js` passes 8 of 8.
No emulator source changed.
